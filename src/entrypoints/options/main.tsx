import React from 'react'
import ReactDOM from 'react-dom/client'
import SignalFeedOptions from '../../ui/options/SignalFeedOptions'
import './style.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SignalFeedOptions />
  </React.StrictMode>,
)
